# Upcoming Tasks

## Next Session
- **Hardcode Opening Moves**: The user requested to revisit hardcoding the first move. Currently, it forces `e4` for White and `e5` for Black blindly. We need to implement proper conditional hardcoding for Black (e.g., only play `e5` against `e4`, play a different book move against `d4` to avoid the Englund Gambit, etc.).
